version: '3'
services:
  dev:
    build:
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - .:/app
    ports:
      - "8080:8080"
  production:
    build: .
    ports:
      - "8080:8080"